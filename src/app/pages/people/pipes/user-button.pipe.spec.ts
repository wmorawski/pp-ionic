import { UserButtonPipe } from './user-button.pipe';

describe('UserButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new UserButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
