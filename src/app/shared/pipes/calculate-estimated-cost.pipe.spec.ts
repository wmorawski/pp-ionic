import { CalculateEstimatedCostPipe } from './calculate-estimated-cost.pipe';

describe('CalculateEstimatedCostPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateEstimatedCostPipe();
    expect(pipe).toBeTruthy();
  });
});
